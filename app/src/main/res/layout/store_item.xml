<?xml version="1.0" encoding="utf-8"?>
<!-- Layout pojedynczego elementu listy sklepów w MainActivity. -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"> <!-- Padding wokół elementu. -->

    <!-- TextView wyświetlający numer sklepu. -->
    <!-- Wycentrowany w poziomie i pionie. -->
    <TextView
        android:id="@+id/storeNameTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center" 
        android:textSize="32sp" 
        android:textStyle="bold" 
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="123" /> <!-- Przykładowy tekst dla podglądu. -->

</androidx.constraintlayout.widget.ConstraintLayout>

